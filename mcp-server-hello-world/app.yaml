# Databricks Apps manifest
# gunicorn auto-reads gunicorn.conf.py from the working directory
command: ["gunicorn", "app:app"]

env:
  - name: PYTHONUNBUFFERED
    value: "1"
  - name: IDLE_TIMEOUT_MINUTES
    value: "30"